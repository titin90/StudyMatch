import 'package:flutter/material.dart';

/// Paleta de colores de la aplicación StudyMatch

// Colores primarios
const Color darkPrimaryColor = Color(0xFF303F9F);     // DARK PRIMARY COLOR
const Color lightPrimaryColor = Color(0xFFC5CAE9);    // LIGHT PRIMARY COLOR  
const Color primaryColor = Color(0xFF3F51B5);         // PRIMARY COLOR

// Colores de acento
const Color accentColor = Color(0xFFFF5722);          // ACCENT COLOR

// Colores de texto
const Color primaryTextColor = Color(0xFF212121);     // PRIMARY TEXT
const Color secondaryTextColor = Color(0xFF757575);   // SECONDARY TEXT

// Colores adicionales
const Color textIconColor = Color(0xFFFFFFFF);        // TEXT / ICONS
const Color dividerColor = Color(0xFFBDBDBD);         // DIVIDER COLOR

// Alias para compatibilidad con código existente
const Color textColor = primaryTextColor;
const Color secondaryColor = accentColor;
const Color grayColor = dividerColor;
